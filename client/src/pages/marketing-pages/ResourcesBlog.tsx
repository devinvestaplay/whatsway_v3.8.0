import { MarketingPageLayout } from "./MarketingPageLayout";
import { marketingPageData } from "./marketingPageData";

export default function ResourcesBlogPage() {
  return <MarketingPageLayout page={marketingPageData.resourcesBlog} />;
}
